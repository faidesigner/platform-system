{
  "name": "platform-system",
  "version": "1.0.0",
  "private": true,
  "description": "Fainders Platform Design System — Monorepo",
  "scripts": {
    "sync": "node scripts/sync-tokens.js",
    "watch": "node scripts/watch-tokens.js"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
